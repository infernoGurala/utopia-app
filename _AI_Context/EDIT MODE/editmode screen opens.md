this is to replace the entire usage of markdown code and use simple pre made setup to use the markdown in simplest way.

- all the code we input using the [[choose type of input]] is divided into blocks in the UI, the user can slide those blocks and can fit them anywhere in the layout and the code will be adjusted accordingly..and pushed to github.